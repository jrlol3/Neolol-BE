// The depth of replies it gets
// So gets the comment and 2 levels of replies
// Each reply search is a query
exports.DEFAULT_COMMENT_DEPTH = 2;

// Score which comments are marked as hidden
// If the comment score is higher than that it's normal again
exports.NEGATIVE_HIDDEN_COMMENT_SCORE = -1;

// Only 10 comments per profile load
exports.MAX_COMMENT_RETRIEVE_ON_PROFILE = 10;
